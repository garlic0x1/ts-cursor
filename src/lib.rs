pub mod cursor;
pub mod file;
pub mod resolved;
pub mod tracer;
pub mod traverser;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
